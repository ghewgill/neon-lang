language: cpp
os:
  - linux
  - osx
compiler:
  - gcc
  #- clang
env:
  - MINIMAL=1 RELEASE=0
  - MINIMAL=1 RELEASE=1
  - MINIMAL=0 RELEASE=0
  - MINIMAL=0 RELEASE=1
before_install:
  - which scons || brew install scons
script: scons MINIMAL=$MINIMAL RELEASE=$RELEASE
sudo: false
