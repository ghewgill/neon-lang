language: cpp
os:
  - linux
  - osx
compiler:
  - gcc
  # It seems that the clang compiler on travis doesn't correctly build its own headers.
  #- clang
env:
  - RELEASE=0
  - RELEASE=1
before_install:
  - which scons || brew install scons
script: scons RELEASE=$RELEASE
sudo: false
