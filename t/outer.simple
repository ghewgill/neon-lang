% TODO SKIP (causes infinite loop)

FUNCTION foo(): Nothing
    VAR outer: String
        FUNCTION bar(): Nothing
            outer := "two"
        END
    outer := "one"
    print(outer)
    bar()
    print(outer)
END

foo()

%= one
%= two
