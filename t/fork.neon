% PLATFORM:posix

IMPORT os
IMPORT sys

VAR p: os.Process := NIL
IF os.fork(OUT p) THEN
    print(str(os.wait(INOUT p)))
    print("parent")
ELSE
    print("child")
    sys.exit(5)
END IF
%= child
%= 5
%= parent
